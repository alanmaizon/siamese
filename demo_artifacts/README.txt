Place example incident artifacts here for quick demos.

Expected files (examples):
- logs (e.g., api.log, worker.log)
- metrics.csv (required for latency anomaly windows)
- docker-compose.yml or docker-compose.yaml
- nginx.conf
- architecture.png (optional)

If you have a mock pack, drop the files in this folder and upload them in the UI.
